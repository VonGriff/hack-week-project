#spring.application.name=hack-week-server

server:
  port: 3000

spring:
  sql:
    init:
      mode: never
      platform: postgresql
  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/lfbgdb
    username: gamer
    password: secret